import React from "react";
import { Link, useParams } from "react-router-dom";
import { usePortalData } from "../../hooks/usePortalData";
import { useGlobalData } from "../../contexts/GlobalDataContext";

const progress: Record<string, number> = { Draft: 12, "Initial Review": 35, "Documents Pending": 45, "Ready for Submission": 55, Submitted: 65, "University Reviewing": 75, "Conditional Offer": 85, "Unconditional Offer": 90, Enrolled: 100 };
export const StudentApplications: React.FC = () => { const { ownApplications } = usePortalData(); return <div className="mx-auto max-w-6xl space-y-6 pb-8"><header><p className="text-sm font-bold text-emerald-700">MY APPLICATIONS</p><h1 className="mt-1 text-3xl font-bold text-slate-900">Application tracker</h1></header><div className="grid gap-4 md:grid-cols-2">{ownApplications.map((app) => <article key={app.id} className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex justify-between gap-3"><div><p className="font-bold text-slate-900">{app.universityName}</p><p className="text-sm text-slate-500">{app.programmeName} · {app.intake}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{app.stage}</span></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-emerald-500" style={{ width: `${progress[app.stage] || 20}%` }} /></div><p className="mt-3 text-sm text-slate-600">{app.nextAction || "No action is required right now."}</p><Link to={`/student/applications/${app.id}`} className="mt-4 inline-block text-sm font-bold text-slate-900">{app.stage === "Draft" ? "Continue application →" : "View application →"}</Link></article>)}</div>{!ownApplications.length && <div className="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-600">You have not started an application. <Link to="/student/universities" className="font-bold text-emerald-700">Explore universities</Link></div>}</div>; };
export const StudentApplicationDetail: React.FC = () => { const { applicationId } = useParams(); const { ownApplications, ownDocuments } = usePortalData(); const { universities } = useGlobalData(); const app = ownApplications.find((item) => item.id === applicationId); if (!app) return <div className="p-8 text-slate-600">Application not found.</div>; const university = universities.find((item) => item.id === app.universityId); return <div className="mx-auto max-w-5xl space-y-6 pb-8"><header className="overflow-hidden rounded-3xl bg-slate-900 p-7 text-white"><p className="text-sm text-emerald-300">{app.applicationNumber}</p><h1 className="mt-2 text-3xl font-bold">{app.universityName}</h1><p className="mt-1 text-slate-300">{app.programmeName} · {app.intake}</p><span className="mt-5 inline-block rounded-full bg-emerald-400/20 px-3 py-1 text-sm font-bold text-emerald-100">{app.stage}</span></header><section className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold text-slate-900">Next action</h2><p className="mt-2 text-sm text-slate-600">{app.nextAction || "Your application is currently being reviewed."}</p></section><section className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold text-slate-900">Application timeline</h2><ol className="mt-4 space-y-4 border-l border-slate-200 pl-5">{(app.history || []).slice().sort((a, b) => b.timestamp - a.timestamp).map((event, index) => <li key={`${event.timestamp}-${index}`}><p className="text-sm font-semibold text-slate-800">{event.stage}</p><p className="text-xs text-slate-500">{new Date(event.timestamp).toLocaleString()} · {event.updatedBy}</p>{event.note && <p className="mt-1 text-sm text-slate-600">{event.note}</p>}</li>)}</ol></section><section className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold text-slate-900">Documents</h2><p className="mt-2 text-sm text-slate-600">{ownDocuments.length} item(s) available in your document vault for this application.</p>{university?.coverImageUrl && <img className="mt-4 h-24 w-40 rounded-xl object-cover" src={university.coverImageUrl} alt={university.coverImageAlt || university.name} />}</section></div>; };
