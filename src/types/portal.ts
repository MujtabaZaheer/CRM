export type VisaCaseStatus = "Preparation" | "Documents Pending" | "Under Review" | "Appointment Scheduled" | "Submitted" | "Granted" | "Refused" | "Escalated";
export interface VisaCase { id: string; studentId: string; studentName: string; applicationId?: string; universityName?: string; country: string; intake?: string; visaType: string; status: VisaCaseStatus; priority: "Low" | "Medium" | "High" | "Urgent"; assignedOfficer?: string; deadline?: string; appointmentAt?: string; notes?: string; history?: { status: VisaCaseStatus; timestamp: number; note?: string; updatedBy?: string }[]; createdAt: number; updatedAt: number; }
export type SupportRequestStatus = "Open" | "In Progress" | "Waiting on Student" | "Resolved" | "Escalated";
export interface SupportRequest { id: string; studentId?: string; studentName: string; applicationId?: string; category: "Student Issue" | "Application Issue" | "Document Issue" | "Technical" | "Other"; priority: "Low" | "Medium" | "High" | "Urgent"; status: SupportRequestStatus; subject: string; description: string; assignedTo?: string; notes?: string; createdAt: number; updatedAt: number; }
